import sqlite3 as lite
import sys

def createTables():
    con = lite.connect('test.db')
    with con:
        cur = con.cursor()
        cur.execute("Create table client(clientId int primary key, name text, phoneNumber char(14), balance int)")
        cur.execute("Insert into client values(0001,'soorej jones','9567711457',0)")
    
